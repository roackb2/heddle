# Unreleased

- Added the independently installable `@roackb2/heddle-adopter` backend SDK.
  It provides the Execution Host v1 contracts, ES256 assertion and optional MCP
  capability issuance, independent product-edge MCP verification, and a strict
  direct HTTP/SSE client without importing Heddle, AWS, the MCP server SDK, or
  product persistence.
